# GitHub 与版本发布准备（尚未执行上传）

目标：GitHub 所有者 `0399199-stack`，仓库名 `EVEING-Clarune`，**私有**。用户确认改用当前登录的 `0399199-stack` 账号；创建和上传前，本源码仓库没有远程地址。不要从源码仓库的上级 `outputs` 或 Codex 工作目录执行上传。

`package.json` 当前版本 `1.0.0-rc.1`，本机完整安装候选包修订为 `fix2`。`fix2` 是同一版本的安装包修订，不等于正式稳定版。程序尚无自动更新机制；官网/更新地址未确定，发布页 URL 不得先写死或在 UI 中假装可用。

## 上传源码前

1. 私有可见性已确定；仍需审定 `LICENSE.md`。当前源码保留所有权、未授予开源再分发许可。若将来改为公开并希望社区可使用/修改/再分发，须由所有者选择并审核新许可，本步骤不代选。
2. 复核 `git status`、`git diff --cached --name-only`、`git log --all`、当前 `.gitignore` 和待提交文件；先完成源码及**全部可推送历史**的敏感信息检查。历史若有秘密，停止并安排凭据轮换及经确认的历史清理，不能只加 ignore。
3. 保留 `package.json`、`pnpm-lock.yaml`、源码、测试、配置、原创品牌资产、许可证与第三方声明。不得提交 `node_modules`、`out`、测试截图、安装包、模型、运行时、签发工具/私钥、客户资料或本机授权状态。
4. 跑测试、类型检查和构建；对最终候选包另外执行安装/卸载、依赖与模型权利、安全、签名及硬件验收。源码能构建不等于可以收费发行。

## 将来发布时（本次不执行）

在上述门禁全部通过并确认 GitHub 仓库后，先选择正式版本号并更新应用版本、锁文件、变更记录与安装器标识；构建可复核的源码提交/标签。完整安装包应从授权来源暂存模型和依赖，保存 SBOM、原始许可、SHA-256 与公钥指纹，检查安装包中没有签发私钥或注册机，再由所有者决定代码签名。最后才在确认的 GitHub 仓库发布对应 Release、安装包、校验摘要和已知限制。若未来加入更新检查，先明确用户授权、签名验证与实际更新 URL；当前没有自动更新服务。

项目已有更细的安装流程见 `INSTALLATION.md`，商用门禁见 `RELEASE_GATES.md`。GitHub 登录、验证码和双重验证必须由所有者本人完成；不要通过聊天传递令牌。
